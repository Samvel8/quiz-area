import { text, defaultLanguage } from "./constants.js";

const warningElement = document.getElementById('warning');
const warningText = text['hy'].warningText;

for (let i = 0; i < warningText.length; i++) {
    
}